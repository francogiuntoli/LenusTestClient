As an FYI, I built different components in Class Components and Functional Components as to demonstrate how I can work with both. Same with styling in useStyles, .css classes and const classes as well with inline styling.

Thanks!
